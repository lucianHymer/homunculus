# GitHub webhook secret for signature verification
# Generate with: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# Server port (optional, defaults to 8080)
PORT=8080

# Workspace directory for cloning repos (Phase 2+)
WORKSPACE_DIR=/workspace

# GitHub App configuration (optional, for better auth)
# Get these from your GitHub App settings page
GITHUB_APP_ID=123456
GITHUB_APP_PRIVATE_KEY_PATH=/path/to/your-app.private-key.pem

# Alternative: Personal Access Token (if not using GitHub App)
# GH_TOKEN=ghp_your_personal_access_token_here